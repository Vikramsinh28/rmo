'use client';

import { ScopeHome } from '@/components/organisms/modules/administration/ScopeHome';

export default function SettingsPage() {
  return (
    <ScopeHome
      title="Settings"
      description="Account details for the signed-in RMO user."
    />
  );
}
